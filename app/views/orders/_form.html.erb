<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :iduser %><br>
    <%= f.number_field :iduser %>
  </div>
  <div class="field">
    <%= f.label :payment_method %><br>
    <%= f.check_box :payment_method %>
  </div>
  <div class="field">
    <%= f.label :date_created %><br>
    <%= f.date_select :date_created %>
  </div>
  <div class="field">
    <%= f.label :date_delivered %><br>
    <%= f.date_select :date_delivered %>
  </div>
  <div class="field">
    <%= f.label :date_paid %><br>
    <%= f.date_select :date_paid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
